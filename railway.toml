[build]
builder = "NIXPACKS"

[deploy]
startCommand = "litellm --config /app/config.yaml --port 4000 --detailed_debug"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3